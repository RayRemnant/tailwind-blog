---
title: How to delete the entire git history from a file
summary: You probably commited some secret key haven't you?

localeid: 'delete-git-file-history'

images: ['/static/images/twitter-card.png']

date: '2024-05-09'
lastmod: '2024-05-09'

tags: ['git', 'repository']
authors: ['default']

language: en
draft: false
---

Inside the terminal, type:

`git filter-branch --tree-filter 'rm -f <yourfile>' HEAD`

`git push origin --force --all`

Then, depending on your operating system...

For Linux:
`rm -rf .git/refs/original/`

For Windows:
`Remove-Item -Path .git\refs\original -Recurse -Force`

And finally..

`git reflog expire --expire=now --all`
`git gc --prune=now --aggressive`
